<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luxuryadmin.mapper.ord.OrdPrintTplMapper">

	<resultMap id="BaseResultMap" type="com.luxuryadmin.entity.ord.OrdPrintTpl">

		<id column="id" jdbcType="INTEGER" property="id" />

		<result column="fk_shp_shop_id" jdbcType="INTEGER" property="fkShpShopId" />

		<result column="context" jdbcType="LONGVARCHAR" property="context" />

		<result column="insert_time" jdbcType="TIMESTAMP" property="insertTime" />

		<result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />

		<result column="insert_admin" jdbcType="INTEGER" property="insertAdmin" />

		<result column="update_admin" jdbcType="INTEGER" property="updateAdmin" />

	</resultMap>

	<sql id="Base_Column_List">
		id,
		fk_shp_shop_id,
		context,
		insert_time,
		update_time,
		insert_admin,
		update_admin
	</sql>

	<insert id="saveObject" parameterType="com.luxuryadmin.entity.ord.OrdPrintTpl" keyProperty="id" useGeneratedKeys="true" >
		insert into ord_print_tpl
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id!=null ">
				id, 
			</if>
			<if test="fkShpShopId!=null ">
				fk_shp_shop_id, 
			</if>
			<if test="context!=null ">
				context, 
			</if>
			<if test="insertTime!=null ">
				insert_time, 
			</if>
			<if test="updateTime!=null ">
				update_time, 
			</if>
			<if test="insertAdmin!=null ">
				insert_admin, 
			</if>
			<if test="updateAdmin!=null ">
				update_admin, 
			</if>

		</trim>
		values
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id!=null">
				#{id,jdbcType=INTEGER},
			</if>
			<if test="fkShpShopId!=null">
				#{fkShpShopId,jdbcType=INTEGER},
			</if>
			<if test="context!=null">
				#{context,jdbcType=LONGVARCHAR},
			</if>
			<if test="insertTime!=null">
				#{insertTime,jdbcType=TIMESTAMP},
			</if>
			<if test="updateTime!=null">
				#{updateTime,jdbcType=TIMESTAMP},
			</if>
			<if test="insertAdmin!=null">
				#{insertAdmin,jdbcType=INTEGER},
			</if>
			<if test="updateAdmin!=null">
				#{updateAdmin,jdbcType=INTEGER},
			</if>

		</trim>

	</insert>

	<delete id="deleteObject" >
		delete from  ord_print_tpl 

		where id = #{id} 
 

	</delete>

	<update id="updateObject" parameterType="com.luxuryadmin.entity.ord.OrdPrintTpl" >
		update ord_print_tpl
		<set>
			<if test="fkShpShopId!=null">
				fk_shp_shop_id = #{fkShpShopId,jdbcType=INTEGER},
			</if>
			<if test="context!=null and context!=''">
				context = #{context,jdbcType=LONGVARCHAR},
			 </if>
			<if test="insertTime!=null">
				insert_time = #{insertTime,jdbcType=TIMESTAMP},
			</if>
			<if test="updateTime!=null">
				update_time = #{updateTime,jdbcType=TIMESTAMP},
			</if>
			<if test="insertAdmin!=null">
				insert_admin = #{insertAdmin,jdbcType=INTEGER},
			</if>
			<if test="updateAdmin!=null">
				update_admin = #{updateAdmin,jdbcType=INTEGER},
			</if>
		</set>
		where id=#{id}

	</update>

	<select id="getObjectById" resultMap="BaseResultMap" >
		select
		id,
		fk_shp_shop_id,
		context,
		insert_time,
		update_time,
		insert_admin,
		update_admin
		from ord_print_tpl
		where id = #{id,jdbcType=INTEGER}

	</select>
    <select id="getTplByShopId" resultType="com.luxuryadmin.entity.ord.OrdPrintTpl"
            parameterType="java.lang.Integer">
		select
		id,
		fk_shp_shop_id,
		context,
		insert_time,
		update_time,
		insert_admin,
		update_admin
		from ord_print_tpl
		where fk_shp_shop_id = #{shopId}
	</select>
	<select id="listTplByShopId" resultType="com.luxuryadmin.entity.ord.OrdPrintTpl"
			parameterType="java.lang.Integer">
		select
		id,
		fk_shp_shop_id as fkShpShopId,
		context,
		insert_time,
		update_time,
		insert_admin,
		update_admin
		from ord_print_tpl
		where fk_shp_shop_id in(-9,#{shopId})
	</select>


</mapper>